#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _strlen - function to find the length of a string
 * @s: input
 * Return: Integer return
 */
int _strlen(char *s)
{
	int value = strlen(*s);

	return (value);
}
