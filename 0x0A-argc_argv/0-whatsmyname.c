#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the name of this file
 * @argc: counts number fo commands entered
 * @argv: strings stored of each command
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", argv[0]);

	return (0);
}
